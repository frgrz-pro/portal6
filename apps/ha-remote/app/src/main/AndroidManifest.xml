<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Client Home Assistant sur le LAN (http://192.168.0.5:8123 : pas de TLS, donc cleartext) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Mini player radio : lecture en arrière-plan (service média au premier plan + notification). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:name=".Portal6App"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@android:style/Theme.Material.NoActionBar">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Tuiles du volet des réglages rapides (la topbar déroulante). -->
        <service
            android:name=".qs.ModesTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_salon"
            android:label="@string/tile_modes"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <service
            android:name=".qs.TvMuteTileService"
            android:exported="true"
            android:icon="@drawable/ic_qs_mute"
            android:label="@string/tile_tv_mute"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- Lecture des flux AzuraCast : survit à la fermeture de l'écran, pilotable depuis la notification. -->
        <service
            android:name=".player.RadioService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>
    </application>

</manifest>
